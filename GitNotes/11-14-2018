Discussed Git Branching Strategy:

https://nvie.com/posts/a-successful-git-branching-model/

Git can handle by both ways:
CLI: Command Line Interface
Manual:


Git Life Cycle:
$ git init			# initialize the git
Suppose for an example you have created a branch and added one file
$ git clone “URL”
Example:
	$ git clone git@github.com:xavyaly/Git.git
	$ git clone https://github.com/xavyaly/Git.git
$ git branch		# List the branch details
	* master
	qa
	st
	pf
	devops
$ touch <file-name>	# created a file; ls -la-> appears in local not in remote
$ git status			# untracked files, appears in red
$ git add <file-name>	# add the file
$ git status			# tracked file, appears in green
$ git pull				# pull the latest changes
$ git pull				# Already up to date.
$ git commit -m “added one normal file to test”	# add a valid message
$ git push			# push the latest changes from local to remote



$ git log			# yellow color is for commit with commit id’s
$ git branch		# list the branches present in local
$ git branch -r	# list the branches in remote
$ git log - -graph	# history of commits list at here
$ git log - -graph - -oneline	# list in one line
$ git log - -help	# 
$ git cat-file -p <commit-id>	# details
$ git cat-file -p <commit-id-tree>	# details

TAG:
$ git tag			# Tags details if present else need to tag
$ git branch
$ git tag -a “0.1”	# tag the branch
$ git push origin - -tags	# reflect in remote, check in release


